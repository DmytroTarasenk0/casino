<!DOCTYPE html>
<html>
<head>
  <title>Slot Machine</title>
</head>
<body>
  <h1>Welcome, <%= user.name %>, to the Slot Machine</h1>
  <p>Your balance: <%= balance %>$</p>

  <form method="POST" action="/slot">
    <label>Enter your bet:</label>
    <input type="number" name="bet" min="1" required />
    <button type="submit">Spin</button>
  </form>

  <% if (spin && spin.length) { %>
    <p>Spin: <%= spin.join(' | ') %></p>
  <% } %>

  <% if (message) { %>
    <p><strong><%= message %></strong></p>
  <% } %>

  <p><a href="/profile">Back to profile</a></p>
</body>
</html>
